extends layout

block content
  .community.lifestyle
    each section,key in sections
      section
        .content
          .text
            h2=section.title
            p !{section.text}
          .image
            img(src="images/community_#{key}.png")
        div(style="clear:both;")
